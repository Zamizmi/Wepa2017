<table class="table table-hover">
  <thead>
    <tr>
      <th>Average Score</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @styles.each do |style| %>
    <tr>
      <td><%= link_to style.name, style %></td>
      <td><%= style.AverageRating%></td>
    </tr>
    <% end %>
  </tbody>
</table>
